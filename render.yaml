services:
  - type: web
    name: agentic-honeypot-simple
    env: python
    plan: free
    buildCommand: pip install fastapi uvicorn
    startCommand: uvicorn simple_api:app --host 0.0.0.0 --port $PORT